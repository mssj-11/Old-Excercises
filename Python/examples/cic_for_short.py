#   Ciclo for y condicional shorts
for i in range(3, 9):   # Retorna una secuencia de numeros entre 3 y 8
    if i % 4 == 0:      # Si cada valor de i es divisible entre 4
        print(i, end=(" "))        # Si se cumple el caso, se imprimiran los numeros divisibles